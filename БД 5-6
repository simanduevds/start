-- 1 задание

select	distinct 
		t1.name
from	users t1 
	inner join t2.orders
		on t1.id = t2.user_id
        
-- 2 задание

select	t1.name
		,t2.name as catalog_name
from	products t1
	inner join catalogs t2
		on t1.catalog_id = t2.id
